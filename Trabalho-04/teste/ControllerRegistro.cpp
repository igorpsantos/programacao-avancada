#include "ControllerRegistro.h"
#include "RegistroDAO.h"
#include "periodoDAO.h"

using namespace std;

ControllerRegistro::ControllerRegistro(){}

void ControllerRegistro::adicionarGastos(Registro registro) {
	// chama o metodo adicionar do RegistroDAO.cpp
}
// no visual studio, essa chamada da erro de declaração incompatível, mas não entendi essa parte, o metodo está declarado correto
void ControllerRegistro::consultarGastos(Periodo periodo) {
	// chama o metodo consultar do periodoDAO.ccp
}